# Example Mixed Parquet csemx Bundle

This variant uses the worked-example geometry and data values, with a distinct
survey identity.

This variant keeps the geometry tables in CSV and writes `data.parquet` instead
of `data.csv`, demonstrating the v1.0 mixed CSV/Parquet table option.

All values are synthetic and not from any real survey.
