# Survey Notes — REPLACE Survey Name

This file is optional, free-form background reading for whoever receives the
bundle. Replace the placeholder text under each heading, add or remove headings
as you like, or delete the whole file if you have nothing to record. Do NOT put
anything here that is *needed* to interpret the data (coordinate systems, sign
conventions, calibrations applied) — that information belongs in
`manifest.yaml` and the data tables, and software will not read this file.

## Instruments and provenance

REPLACE: what equipment acquired the data — transmitter and receiver types or
models, electrode/coil configurations, turn counts that were normalized out,
and anything notable about the deployment.

## Processing summary

REPLACE: a short narrative of how the delivered responses were produced —
processing software and steps, stacking/averaging, calibration and declination
corrections applied before delivery, and how the error estimates were derived.

## QC remarks

REPLACE: data-quality remarks — noisy sites or frequencies, why any rows are
flagged `use = 0`, repeatability checks, and anything the consumer should know
before modeling.
